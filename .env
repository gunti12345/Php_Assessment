# Optional: add a GitHub token to avoid rate limits.
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=

# How many repositories to fetch per refresh (max 100)
GITHUB_PER_PAGE=50

# Database settings (match docker-compose defaults)
DB_HOST=db
DB_PORT=3306
DB_DATABASE=app
DB_USERNAME=app
DB_PASSWORD=app
